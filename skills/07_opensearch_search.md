# OpenSearch (Search & Discovery)

- Index mapping (keyword/text/numeric), analyzers for names/addresses/unit codes
- Filter queries with org_id guard rails
- Denormalization strategy and reindex workflow
- Search flow: OpenSearch IDs → fetch docs from Redis/Mongo
