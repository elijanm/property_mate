# Backend Engineering (FastAPI Async)

- Async IO patterns, avoiding blocking calls
- Service/repository layering, dependency injection
- REST API design: pagination, filtering, versioning
- Multi-tenant request scoping (org_id enforcement everywhere)
- RBAC: org roles + property-scoped permissions
- Input validation with shared schemas (packages/shared)
