# Redis (Cache, Locks, Idempotency, Rate Limiting)

- Cache-aside patterns and invalidation events
- Idempotency key strategy (namespace, TTL, collision avoidance)
- Distributed locks (unit assignment, billing runs, settlements)
- Rate limiting (token bucket/sliding window) per org/user/ip
- Monitoring: hit ratio, lock contention, memory pressure
